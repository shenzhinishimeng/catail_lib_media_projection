apply plugin: 'com.android.library'

android {
    compileSdkVersion 31

    defaultConfig {
        minSdkVersion 21
        targetSdkVersion 31
    }

}

dependencies {
    implementation fileTree(include: ['*.jar'], dir: 'libs')
}
